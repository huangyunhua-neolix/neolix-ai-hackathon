[project]
name = "neolix-agent"
version = "0.1.0"
description = "Minimal Feishu agent bot powered by Claude"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.45.0",
    "lark-oapi>=1.5.0,<2.0.0",
]
